import { Injectable } from '@angular/core';
import { Observable, Observer } from 'rxjs';
import { Dish } from '../services/datatypes';

@Injectable({
  providedIn: 'root'
})
export class Store {

  constructor() { }
}
